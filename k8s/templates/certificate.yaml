apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: shows-tls
  namespace: shows
spec:
  secretName: shows-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - shows.romaine.life
