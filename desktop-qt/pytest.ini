[pytest]
# Put desktop-qt/ on sys.path so tests import the `shows` package; only the
# pure modules (roundlogic, apiclient) are exercised — nothing that imports
# Qt or libmpv — so these run on a plain Python runtime in CI.
pythonpath = .
testpaths = tests
